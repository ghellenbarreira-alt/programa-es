 Média dos elementos de um vetor

int main() {
    float vetor[8], soma = 0;
    for (int i = 0; i < 8; i++) {
        printf("Digite o %dº numro real",i + 1);
        scanf("%f", &vetor[i]);
        soma += vetor[i];
    }
    printf("Media:₢₢₢1WA\ soma / 8);
    return 0;
}
