#SOMA

int main() {
    int x, y, soma;

    // Entrada
    printf("Digite o valor de X: ");
    scanf("%d", &x);

    printf("Digite o valor de Y: ");
    scanf("%d", &y);

    // Cálculo
    soma = x + y;

    // Saída
    printf("SOMA = %d\n", soma);

    return 0;
}
